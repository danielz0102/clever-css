---
description: Orchestrate the execution of a plan delegating tasks to subagents
agent: build
---

You're the orchestrator of this plan. Delegate each task to a subagent, taking care of executing each one in the correct order. Don't execute a task if it depends on another that isn't completed yet.

Mark acceptance criteria, and report if something goes wrong. If some task fails, abort the plan and report it.

The plan is the following:

$ARGUMENTS
